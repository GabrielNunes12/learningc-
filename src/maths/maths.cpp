//
// Created by Gabriel Nunes on 11/16/2023.
//

#include <cmath>
#include <iostream>
int main() {
    std::max(1,4); //will return 4;
    std::min(0,2); // will return 0;
    //cmath functions (https://cplusplus.com/reference/cmath/)
    pow(2,3);
    sqrt(9);
    abs(-44);
    round(3.11);
    ceil(4.20);
    floor(3.99);
    return 0;
}